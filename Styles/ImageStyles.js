import { StyleSheet } from 'react-native';

export const ImageStyles = StyleSheet.create({
  backgroundImg: {
    height: "100%",
    width: "100%",
    backgroundColor: "rgba(253,253,241,0.82)"
  }
})
